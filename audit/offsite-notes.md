# Off-site notes

Notes on off-site, non-code work items that support the on-site AI-search
audit but are not themselves file changes to review in a PR.

## IndexNow key

- Key: `bcd424e6a5643855bb548dce9676fc59`
- Key file: `/bcd424e6a5643855bb548dce9676fc59.txt` (repo root, file content is
  the key itself, per the IndexNow convention).
- Submitting the key/URL list to the Bing IndexNow endpoint (and any other
  IndexNow-participating search engine) is an off-site checklist item, not
  done as part of this commit. It is a follow-up action for whoever has
  operational access to run the submission (a simple HTTP call to
  `https://api.indexnow.org/indexnow` or `https://www.bing.com/indexnow`
  with the key, key location, and the list of URLs to (re)index).

## llms-full.txt regeneration dependency

- `llms-full.txt` is generated by `scripts/generate-llms-full.py` from the
  live text of every page it lists. It does not read `site-config.json`;
  it only copies whatever text is already on each page.
- The "NN Google reviews" trust line is one such piece of copied text, and
  it is materialized 53 times across the site (once per marked page).
  Whenever `update-review-count.py` is run and changes the review count or
  rating, `scripts/generate-llms-full.py --apply` must be re-run afterward
  so `llms-full.txt` reflects the same numbers as the live pages. This is a
  manual, sequential dependency: regenerate the count first, then
  regenerate llms-full.txt.

## Numbers data file

- `site-config.json` is the single source of truth for the review counts
  and the published tier and onboarding prices. It now holds two top-level
  sections:
  - The existing review keys: `google_review_count`, `google_rating`.
  - A `pricing` object: `tiny_team_monthly` (600), `core_per_user` (125),
    `secure_growth_per_user` (175), `compliance_continuity_per_user` (250),
    `onboarding_tiny_team` (1000), `onboarding_5_to_15` (1500), an
    `onboarding_scope_note` string describing the Microsoft 365 default
    scope, and `incident_notification_hours` (4).
- Two scripts read this file; a third depends on them having been run
  first:
  - `scripts/update-review-count.py` rewrites the marked review-count and
    rating trust blocks from the `google_review_count` / `google_rating`
    keys.
  - `scripts/update-site-numbers.py --check` scans root `*.html` and
    `blog/*.html` for dollar figures anchored to the four tier names
    (Tiny Team, Core Managed IT, Secure Growth, Compliance & Continuity)
    and to the two onboarding fees, and reports anything that does not
    match the `pricing` object, including inside JSON-LD Offer blocks. It
    also reports (never rewrites) `assets/js/pricing.js`'s `TIER_PRICES`
    constant against the same config. `--apply` rewrites only the
    confidently-anchored dollar figures it finds mismatched; everything
    else is left for a human to fix and re-run the checker. As of this
    pass, `--check` reports zero mismatches: every published price on the
    site already matches `site-config.json`.
  - `scripts/generate-llms-full.py` does not read `site-config.json`
    itself (see the note above); it only copies whatever pricing text is
    currently live on each page, so it depends on the other two scripts
    having already been run first.
- Update workflow when a real price or fee changes: edit the `pricing`
  object in `site-config.json`, run `update-site-numbers.py --apply` to
  rewrite the confidently-anchored occurrences, hand-fix anything the
  `--check` output flags as a low-confidence or ignored match (the script
  deliberately does not touch those), update `assets/js/pricing.js`'s
  `TIER_PRICES` by hand since it is report-only, run `scripts/minify.py
  --refs` to regenerate `pricing.min.js`, then run
  `scripts/generate-llms-full.py --apply` to bring `llms-full.txt` back in
  sync, then redeploy.

## Final P1 verification sweep (2026-08-25)

Closing verification pass for the P1 fix series (fixes 1 through 20).

- **trucking-it-services.html**: the meta description and og:description
  still carried a soft response-time claim ("fast response") alongside
  "federal-grade engineering, transparent pricing." Reworded both to
  "federal-grade engineering, direct access to the owner, transparent
  pricing," dropping the speed promise per D4 without touching the
  page body or any other line.
- **llms-full.txt regeneration**: ran `scripts/generate-llms-full.py
  --apply`. Re-ran a second time and diffed; the two outputs are
  byte-identical (confirmed by `sha256sum`), so generation is
  deterministic. The `. federal-grade` broken-sentence fragments (a
  colon turned into a period by an earlier edit, on the agriculture,
  trucking, manufacturing, healthcare, professional-services, and
  property-management pages) are gone from the regenerated file; all
  now read "...Central Coast: federal-grade engineering..." with the
  colon intact. The trucking fix above is reflected. The regeneration
  also picked up the hospitality-it-services.html page content (not
  previously captured in llms-full.txt) and the Gilroy-area county
  trust-line fixes (Santa Cruz/San Benito/Santa Clara County wording
  replacing leftover "Monterey County" copy on out-of-county pages).
  No other content changes appeared in the diff.
- **sitemap.xml regeneration**: ran `scripts/generate-sitemap.py`.
  Output is byte-identical to the prior committed sitemap.xml (no
  diff at all, not even a lastmod change).
- **update-site-numbers.py --check**: 188 ok, 11 ok (long-distance
  match), 0 mismatches, 2 ignored (non-onboarding headcount blurbs on
  pricing.html). JSON-LD Offer prices: 16 ok, 0 mismatches. Result:
  every published price matches `site-config.json`.
- **update-review-count.py --root . (dry run)**: 56 files scanned, all
  reported "already up to date," 0 fields updated.
- **Banned-term sweep** across root `*.html`, `blog/*.html`, `llms.txt`,
  `llms-full.txt`, `feed.xml`, `sitemap.xml`:
  - Em dash (literal, `%E2%80%94`, `&mdash;`): 0 hits.
  - dental/dentist (word boundary): 0 hits.
  - Cisco: only in reader-advice blog copy (Ubiquiti/Cisco
    Meraki/Aruba access-point pricing in
    `blog/business-wifi-network-segmentation-small-business-2026.html`)
    and in our-stack equipment mentions already carrying an inline
    `<!-- [VERIFY] -->` comment tag (managed-it-services.html,
    network-design.html, and their JSON-LD mirrors). No Cisco
    certification claims found anywhere. Note: the four Cisco
    Meraki/Catalyst lines that carry into llms-full.txt (lines ~52,
    ~493, ~495, ~512) read as plain, untagged text there because the
    generator strips all HTML comments, [VERIFY] included, before
    extracting page text; see the generator-scope note below for the
    full extent of this.
  - SIEM: 4 hits, all definitional/educational blog context, not a
    Ghosxt capability claim: `blog/cybersecurity-services-monterey-2026.html`
    (advising against buying enterprise SIEM for a 25-person firm) and
    `blog/it-support-vs-managed-it-services-difference.html` (MSP vs.
    MSSP glossary, three occurrences). ITDR: 0 hits.
  - Huntress/Veeam/Datto/Wasabi/Backblaze/pfSense/OPNsense/WatchGuard/
    Aruba/Hyper-V/VMware/SentinelOne/CrowdStrike/Sophos/UniFi: all
    hits confined to `blog/*.html` reader-advice content (EDR/backup
    product comparisons, CVE write-ups, Wi-Fi segmentation hardware).
    Zero hits in any root `*.html`, llms.txt, llms-full.txt, feed.xml,
    or sitemap.xml, confirming no vendor name describes Ghosxt's own
    stack anywhere those files are served.
  - Clearance-level terms (Secret/Top Secret/TS-SCI as a clearance
    descriptor): 0 hits. The only "secret" hits are cryptographic
    usage (passkeys, WordPress salts) in blog posts, unrelated to
    clearance.
  - "Senior Solutions Consultant", "40+ Central Coast businesses",
    "multinational corporation", "10+ years": 0 hits.
  - Response-time promises: initial pass found two files still
    carrying "fast response" outside this ticket's original scope:
    `help-desk-it-support.html` (meta description, og:description,
    twitter:description, JSON-LD description, and one body paragraph)
    and `services.html` line 234 (help-desk summary card). Both were
    then PM-authorized as a same-class follow-up and fixed with the
    same direct-owner-access framing used on trucking-it-services.html:
    - `help-desk-it-support.html`: meta description, og:description,
      and twitter:description now read "...live US-based help, direct
      access to the owner, remote & on-site..." (and the two shorter
      variants); the JSON-LD Service description now reads
      "...remote and on-site troubleshooting, direct access to the
      owner, and a single senior engineer who knows your
      environment..."; the body paragraph now reads "the friendly,
      direct line to the owner when something is wrong."
    - `services.html` line 234: "Live, US-based IT help desk and
      end-user support: direct access to the owner, remote and on-site
      fixes, and a real engineer who knows your environment..."
    - `llms-full.txt` was regenerated again after these two fixes and
      re-diffed against the prior version: the only three lines that
      changed are the help-desk-it-support.html meta description, its
      body paragraph, and the services.html card text, exactly
      matching the source edits. Re-ran the generator twice more and
      confirmed byte-identical output (sha256 match) each time.
    - Two further hits were then found and PM-authorized as a closing
      round of the same D4 class:
      `blog/it-help-help-desk-tech-support-small-business-salinas.html`
      line 45 (JSON-LD description: "What Ghosxt actually does when
      you call... Flat-rate pricing, fast response, on-site..."), a
      genuine first-person Ghosxt claim rather than reader-advice, and
      `help-desk-it-support.html`'s second body paragraph ("plus the
      human support your team can reach in seconds"), a related
      in-seconds response promise sitting next to the line already
      fixed. No visible mirror of the blog post's JSON-LD description
      text exists on the page (the body content under "What Ghosxt
      actually does when you call" is a bulleted problem list, not a
      restatement of the summary sentence), so only the JSON-LD needed
      changing. Both were reworded:
      - `blog/it-help-help-desk-tech-support-small-business-salinas.html`
        JSON-LD description now reads "...Flat-rate pricing, direct
        access to the owner, on-site without travel charges."
      - `help-desk-it-support.html`'s paragraph now reads "...plus
        direct access to the owner when your team needs help."
    - `llms-full.txt` was regenerated a third time (individual blog
      posts are not captured in llms-full.txt content, only linked
      from the blog-index section per the generator's design, so the
      blog-post JSON-LD edit does not appear there). Re-diffed against
      the prior version: the only line that changed is the
      help-desk-it-support.html paragraph, exactly matching the source
      edit. Re-ran the generator twice more and confirmed
      byte-identical output (sha256 match) each time.
    - Final grep of "fast response" across root `*.html`, `blog/*.html`,
      `llms.txt`, `llms-full.txt`, `feed.xml`, and `sitemap.xml`: one
      hit remains, in
      `blog/holiday-season-cybersecurity-checklist-small-business-2026.html`
      ("support teams run thinner right when a fast response matters
      most"), generic seasonal-industry commentary and not a
      first-person Ghosxt claim; reader-advice context, no action
      needed.
    - Final grep of "in seconds" across the same file set: the fixed
      line is gone. Remaining hits are all either automated-technology
      descriptions in blog posts and one root case study (EDR/MDR
      products isolating an endpoint "within seconds,"
      `healthcare-it-services.html:542` and
      `blog/ai-attack-speed-22-seconds-mdr-smb.html`,
      `blog/edr-vs-antivirus-small-business-2026.html`, describing what
      the deployed security tooling does automatically, not a human
      support-response promise), attacker-behavior descriptions
      (`blog/usb-drop-attacks-small-business-2026.html`,
      `blog/wordpress-funnel-builder-compromise-smb-website-2026.html`),
      or unrelated context (fact-verification speed in
      `blog/top-managed-it-providers-salinas-monterey-bay.html` and
      `blog/top-website-design-companies-salinas-monterey-bay.html`;
      website-visitor decision speed on the eleven `web-design-*.html`
      pages and their `llms-full.txt` mirrors, "your customers decide
      from a phone, in seconds, based on how fast and clear your site
      is," a UX/marketing point about the reader's own site, not a
      Ghosxt support-response claim). None of these are D4 response-
      time promises about Ghosxt's own support or incident handling.
    - Everything else that matched "same day / same-hour / 24-48
      hours" search terms was one of the two allowed exclusions:
      security-control offboarding process descriptions (employee/
      seasonal-worker offboarding checklists, BAA vendor-list
      production, professional-services and property-management
      offboarding FAQ answers) or third-party/generic-industry content
      (Microsoft's Patch Tuesday release cadence, PG&E PSPS timing,
      "what MSPs typically charge for same-day on-site response,"
      cyber-insurance-carrier guidance on the first 24-48 hours of an
      incident). One borderline case reviewed and judged not a
      violation: trucking-it-services.html's case study line "a
      silent failure now pages someone within the hour" describes an
      internal monitoring alert, not a promise to the client.
  - No "instant response," "guaranteed response," "1-hour response,"
    or similar SLA language found anywhere.
- **Structural checks**: every one of the 215 HTML pages (97 root
  `*.html` plus 118 `blog/*.html`) has exactly one `<h1>` (zero
  exceptions) and a `rel="canonical"` link (zero missing). The banned-
  term sweep's file set was 219 files total: those same 215 HTML pages
  plus 4 non-HTML served files (`llms.txt`, `llms-full.txt`, `feed.xml`,
  `sitemap.xml`), which are outside the H1/canonical check since they
  are not HTML documents. Internal link check across all root and blog
  pages (20,210 `href="/..."` links, resolved against clean-URL,
  `.html`, and `index.html` conventions) found zero broken links. All
  `https://ghosxt.com/...` links inside llms.txt also resolve to an
  existing page.

Fix 20 closed: all "fast response" and in-seconds response-time wording
identified during this sweep has now been reworded to the direct-
owner-access framing, across trucking-it-services.html,
help-desk-it-support.html, services.html, and
blog/it-help-help-desk-tech-support-small-business-salinas.html. The one
remaining "fast response" hit (the holiday-season blog post) is generic
reader-advice, not a Ghosxt claim, and was left as-is.

Code-review response, second round: the reviewer identified three
anonymized case-study sentences describing immutability claims that
were untagged while their sibling claims elsewhere on the same pages
already carried `<!-- [VERIFY] -->`. Per CLAUDE.md, anonymized case
examples are not to be rewritten or removed without Uli, so only the
comment tag was appended in each case, with the sentence wording left
byte-for-byte unchanged:
- `healthcare-it-services.html` line 542: tag appended after "...fully
  immutable." (mirroring line 419's end-of-sentence convention),
  mid-paragraph before "We rebuilt the affected workstation...".
- `manufacturing-it-services.html` line 544: tag appended at the end
  of the paragraph, after "...silent failure can ever happen again."
- `engineering-it-services.html` line 580: tag appended at the end of
  the paragraph, after "...within the project's recovery time
  objective." (line 723 on the same page already used the tag
  elsewhere in the file, establishing precedent for its use here).
`llms-full.txt` was regenerated with `--apply`; because the tags are
HTML comments and the generator strips comments before extracting
text, the regenerated file is byte-identical to the version before
these three edits (confirmed by diff). Re-ran the generator twice more
and confirmed byte-identical output via sha256 each time, so
determinism holds. `git diff` on all three edited HTML files shows only
the appended `<!-- [VERIFY] -->` comment text was added; no other
character changed.

Standing decision item for Uli, separate from D4, not fixed under this
ticket: generate-llms-full.py strips all HTML comments (including every
`<!-- [VERIFY] -->` tag) before extracting page text, so llms-full.txt
contains **zero** VERIFY markers while the source HTML currently carries
88 such tags across 29 root *.html files, plus 1 more tag in 1 blog
file, 89 across 30 HTML files in total (verified by direct grep after
this round's three additions to healthcare-it-services.html,
manufacturing-it-services.html, and engineering-it-services.html; the
root count was 85 tags across 28 files immediately before those three
edits). Every VERIFY-tagged claim site-wide therefore reads as a plain,
unqualified assertion in the derived file, with
backup-disaster-recovery.html's immutable-backup claims the most
exposed (12 of the 88 tags, the single densest concentration on the
site, all read as plain fact in llms-full.txt). This is a generator-
design question, not a response-time or wording issue, and options for
Uli are: (a) leave the generator as-is and accept that llms-full.txt
never carries the VERIFY qualifier, or (b) extend
generate-llms-full.py to detect `[VERIFY]` inside an HTML comment
specifically and preserve it (for example as a trailing "[VERIFY]" or
"(unverified)" marker) instead of silently discarding it with the rest
of the comment stream. No fix made; flagging for a decision.
